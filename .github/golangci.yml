version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - unconvert
    - unparam
    - misspell
  exclusions:
    generated: lax
    paths:
      - spl_lexer\.go
      - spl_parser\.go
      - splparser_base_listener\.go
      - splparser_base_visitor\.go
      - splparser_listener\.go
      - splparser_visitor\.go
  settings:
    misspell:
      locale: US

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/craftedsignal/spl-parser
